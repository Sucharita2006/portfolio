// Phase 1 scaffold only, so that `next build` has a root route to compile and
// `npm run dev` serves something. Replaced wholesale by the hero, work index,
// capabilities, and contact sections in Phase 5.
export default function Home() {
  return (
    <div className="mx-auto max-w-5xl px-6 py-24">
      <h1 className="text-2xl">Sucharita Chattopadhyay</h1>
      <p className="mt-2 text-sm">Scaffold. Phase 1 of 10.</p>
    </div>
  );
}
