// Scaffold only, so `next build` has a root route to compile and `npm run dev`
// serves something. Replaced wholesale by the hero, work index, capabilities,
// and contact sections in Phase 5.
export default function Home() {
  return (
    <div className="shell py-24">
      <h1 className="font-display text-hero">Sucharita Chattopadhyay</h1>
      <p className="eyebrow mt-4">Scaffold · phase 2 of 10</p>
    </div>
  );
}
